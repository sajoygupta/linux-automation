global:
  scrape_interval: 15s # প্রতি ১৫ সেকেন্ডে সার্ভার মেট্রিক্স কালেক্ট করবে

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100'] # Node Exporter Port
